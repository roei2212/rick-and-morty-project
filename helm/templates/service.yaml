apiVersion: v1
kind: Service
metadata:
  name: {{ include "rickmorty-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "rickmorty-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000